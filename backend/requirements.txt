# Backend dependencies for always-stock
# Install: pip install -r requirements.txt

# Web framework
fastapi>=0.111.0
uvicorn[standard]>=0.30.0

# Database ORM
sqlalchemy>=2.0.0
psycopg[binary]>=3.1.0

# Data validation (FastAPI dependency)
pydantic>=2.0.0

# Telegram bot
python-telegram-bot>=21.0

# OpenAI (AI analyst sub-agent)
openai>=1.0.0

# HTTP client (used by market router for Yahoo Finance)
requests>=2.31.0

# Environment config
python-dotenv>=1.0.0

# FinMind SDK (ETL 資料源)
FinMind>=0.8.0
pandas>=1.5.0

# Dev / test
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.27.0   # FastAPI TestClient dependency
